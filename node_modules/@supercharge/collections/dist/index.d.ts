import { PendingAsyncCollection } from './pending-async-collection';
/**
 * Create a new collection for the given `items`. The `items`
 * argument can be a single value or an existing array.
 *
 * @param {*} items
 *
 * @returns {PendingAsyncCollection}
 */
declare const collect: <T>(collection?: T | T[] | undefined) => PendingAsyncCollection<T>;
export default collect;
export declare const Collect: <T>(collection?: T | T[] | undefined) => PendingAsyncCollection<T>;
